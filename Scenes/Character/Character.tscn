[gd_scene load_steps=12 format=2]

[ext_resource path="res://Scenes/Character/Assets/face/face-bored.png" type="Texture" id=1]
[ext_resource path="res://Scenes/Character/Character.gd" type="Script" id=2]
[ext_resource path="res://Scenes/Character/Assets/body/body-bear.png" type="Texture" id=3]
[ext_resource path="res://Scenes/Character/Assets/arms/arms-bend.png" type="Texture" id=4]
[ext_resource path="res://Scenes/Character/Assets/legs/legs-blob0.png" type="Texture" id=5]
[ext_resource path="res://Scenes/Character/Assets/facial-hair/facial-moustacheA.png" type="Texture" id=6]
[ext_resource path="res://Scenes/Character/Assets/head/hat-arrow.png" type="Texture" id=7]

[sub_resource type="Shader" id=1]
code = "shader_type canvas_item;
render_mode unshaded;
 
uniform float width : hint_range(0.0, 16.0);
uniform vec4 outline_color : hint_color;
 
void fragment()
{
    vec2 size = vec2(width) / vec2(textureSize(TEXTURE, 0));
   
    vec4 sprite_color = texture(TEXTURE, UV);
   
    float alpha = sprite_color.a;
    alpha += texture(TEXTURE, UV + vec2(0.0, -size.y)).a;
    alpha += texture(TEXTURE, UV + vec2(size.x, -size.y)).a;
    alpha += texture(TEXTURE, UV + vec2(size.x, 0.0)).a;
    alpha += texture(TEXTURE, UV + vec2(size.x, size.y)).a;
    alpha += texture(TEXTURE, UV + vec2(0.0, size.y)).a;
    alpha += texture(TEXTURE, UV + vec2(-size.x, size.y)).a;
    alpha += texture(TEXTURE, UV + vec2(-size.x, 0.0)).a;
    alpha += texture(TEXTURE, UV + vec2(-size.x, -size.y)).a;
   
    vec3 final_color = mix(outline_color.rgb, sprite_color.rgb, sprite_color.a);
    COLOR = vec4(final_color, clamp(alpha, 0.0, 1.0));
}"

[sub_resource type="ShaderMaterial" id=2]
shader = SubResource( 1 )
shader_param/width = null
shader_param/outline_color = null

[sub_resource type="Shader" id=3]
code = "shader_type canvas_item;
render_mode unshaded;
 
uniform float width : hint_range(0.0, 16.0);
uniform vec4 outline_color : hint_color;
 
void fragment()
{
    vec2 size = vec2(width) / vec2(textureSize(TEXTURE, 0));
   
    vec4 sprite_color = texture(TEXTURE, UV);
   
    float alpha = sprite_color.a;
    alpha += texture(TEXTURE, UV + vec2(0.0, -size.y)).a;
    alpha += texture(TEXTURE, UV + vec2(size.x, -size.y)).a;
    alpha += texture(TEXTURE, UV + vec2(size.x, 0.0)).a;
    alpha += texture(TEXTURE, UV + vec2(size.x, size.y)).a;
    alpha += texture(TEXTURE, UV + vec2(0.0, size.y)).a;
    alpha += texture(TEXTURE, UV + vec2(-size.x, size.y)).a;
    alpha += texture(TEXTURE, UV + vec2(-size.x, 0.0)).a;
    alpha += texture(TEXTURE, UV + vec2(-size.x, -size.y)).a;
   
    vec3 final_color = mix(outline_color.rgb, sprite_color.rgb, sprite_color.a);
    COLOR = vec4(final_color, clamp(alpha, 0.0, 1.0));
}"

[sub_resource type="ShaderMaterial" id=4]
shader = SubResource( 3 )
shader_param/width = null
shader_param/outline_color = null

[node name="Character" type="Node2D"]
material = SubResource( 2 )
script = ExtResource( 2 )

[node name="Body" type="Sprite" parent="."]
material = SubResource( 4 )
position = Vector2( 0, -12 )
texture = ExtResource( 3 )

[node name="Head" type="Sprite" parent="."]
position = Vector2( 0, -12 )
texture = ExtResource( 7 )

[node name="Face" type="Sprite" parent="."]
position = Vector2( 1, -11 )
texture = ExtResource( 1 )

[node name="FacialHair" type="Sprite" parent="."]
position = Vector2( 1, -13 )
texture = ExtResource( 6 )

[node name="Arms" type="Sprite" parent="."]
position = Vector2( 0, -13 )
texture = ExtResource( 4 )

[node name="Legs" type="Sprite" parent="."]
position = Vector2( 0, -12 )
texture = ExtResource( 5 )
